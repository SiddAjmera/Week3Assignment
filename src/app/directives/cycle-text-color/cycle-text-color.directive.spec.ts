import { CycleTextColorDirective } from './cycle-text-color.directive';

describe('CycleTextColorDirective', () => {
  it('should create an instance', () => {
    const directive = new CycleTextColorDirective();
    expect(directive).toBeTruthy();
  });
});
