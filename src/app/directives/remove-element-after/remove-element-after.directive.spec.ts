import { RemoveElementAfterDirective } from './remove-element-after.directive';

describe('RemoveElementAfterDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveElementAfterDirective();
    expect(directive).toBeTruthy();
  });
});
